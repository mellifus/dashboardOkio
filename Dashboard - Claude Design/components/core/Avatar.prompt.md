Circular initials avatar for the signed-in user, or a soft tinted placeholder circle for clients without a photo.

```jsx
<Avatar initials="MR" size={30} tone="accent" />
<Avatar size={52} tone="tint" />
```
