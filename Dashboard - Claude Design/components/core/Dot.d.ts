export interface DotProps {
  /** Any CSS color — typically a category color (reschedule, price inquiry, medical concern, etc). */
  color?: string;
  /** Diameter in px. 5-6 for list rows, 7-8 for headers. */
  size?: number;
}
