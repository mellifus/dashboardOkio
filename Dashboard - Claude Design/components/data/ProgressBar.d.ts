/**
 * @startingPoint section="Data" subtitle="Labeled percentage bar for rankings/utilization" viewport="700x140"
 */
export interface ProgressBarProps {
  label: string;
  /** 0-100. */
  pct: number;
}
