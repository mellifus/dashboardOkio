Underline-style tab row — client profile sections (Resumen/Historial/Fotos/Notas/Consentimiento).

```jsx
<Tabs tabs={[{id:"overview",label:"Resumen"},{id:"history",label:"Historial"}]} activeId="overview" onChange={setTab} />
```
